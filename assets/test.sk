on load:
 # set {_res} to 25 / 1
 # send {_res} to console
  set {_m} to 0
  loop 12 times:
    set {_m::%loop-value%} to %loop-value% + 1
  set {_size} to nativecall maplen {_m}
  send {_size} to console
  loop {_m::*}:
    send "value is" and %loop-value% to console
