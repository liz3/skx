
on load:
	loop 15 times:
		if %loop-value% is 5:
			set {list::%loop-value%} to "test 33"
		else:
			set {list::%loop-value%} to "test whatever"
	loop 15 times:
		send %loop-value% to console
		if {list::%loop-value%} is "test 33":
			send "its test 5" to console
		else:
			send "its something" to console