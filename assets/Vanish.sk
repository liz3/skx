on load:
	loop 15 times:
		if %loop-value% smaller then 4:
			set {list::%loop-value%} to 12
		else:
			set {list::%loop-value%} to 22
	loop 15 times:
		if {list::%loop-value%} is 12:
			send {list::%loop-value%} to console
		else:
			send {list::%loop-value%} and %loop-value% to console