
function test():
	loop 15 times:
		if %loop-value% smaller then 4:
			set {list::%loop-value%} to 12
		else:
			set {list::%loop-value%} to 22
	loop {list::*}:
		send "test" to console

on load:
	loop 5 times:
		test()
		send "=====" to console