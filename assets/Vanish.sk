
on load:
	loop 15 times:
		send %loop-value% to console
		if %loop-value% is 5:
			set {list::%loop-value%} to "test 5"
		else:
			set {list::%loop-value%} to "test whatever"
	loop {list::*}:
		send %loop-value% and %loop-index% to console